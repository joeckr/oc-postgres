name: CI

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  matrix-build:
    uses: joeckr/ci-templates/.github/workflows/build-oc-modified.yml@main
    with:
      image: "postgres"
    secrets: inherit
